# OrangeHRM Quality Lab - 项目协作说明

## 一、项目目标

本仓库是一个基于 OrangeHRM 的测试开发学习项目。

项目主要用于：

- 系统学习软件测试开发相关技术；
- 完成一个能够真实运行、能够在面试中完整讲解的测试项目；
- 掌握接口自动化、UI 自动化、数据库校验、持续集成及 AI 辅助测试等能力；
- 为测试开发岗位求职和面试做准备。

本项目的目标不仅是“把代码跑通”，还要求理解每一个重要设计：

- 为什么这样设计；
- 解决了什么问题；
- 是否存在其他方案；
- 面试时如何解释。

---

## 二、文档职责与任务开始前读取顺序

### 1. 文档职责

- `AGENTS.md`：维护长期稳定、可执行的项目协作规则，不记录详细 Phase 进度或临时阶段限制；
- `docs/ROADMAP.md`：项目阶段、已完成内容和下一阶段计划的唯一事实来源；
- `docs/LEARNING_LOG.md`：记录真实问题、排查过程、新理解、认知纠正和有面试价值的实践；
- `README.md`：对外说明项目范围、当前成果、技术栈和文档入口，内容应与 ROADMAP 中的实际进度一致。

技术栈是否已经实践、当前处于哪个 Phase，以及下一步学习什么，统一先查看 `docs/ROADMAP.md`，不要在 `AGENTS.md` 中重复维护状态快照。

### 2. 任务开始前读取顺序

开始任务前，按以下顺序了解上下文：

1. `AGENTS.md`；
2. `docs/ROADMAP.md`；
3. `docs/LEARNING_LOG.md`；
4. 当前任务相关代码，包括对应测试、配置和测试数据。

完成上述三个通用文档的阅读后，只扩展读取与当前任务相关的代码、测试、配置和数据；如文档之间出现阶段状态不一致，以 `docs/ROADMAP.md` 为准，并在任务范围允许时同步相关说明。

---

## 三、项目开发原则

### 1. 小步迭代

每次只完成一个明确的小目标，在已有实践基础上逐步演进。不要一次性生成完整自动化框架，也不要为未来可能出现的需求提前增加复杂结构。

---

### 2. 修改文件前先说明

修改前需要说明：

1. 准备修改什么；
2. 为什么需要修改；
3. 会影响哪些文件；
4. 修改完成后如何验证。

---

### 3. 简单、清晰、可解释优先

本项目是学习和求职项目。

代码和文档优先考虑：

- 清晰；
- 易懂；
- 容易解释；
- 容易维护。

不要为了体现“高级”而加入没有必要的：

- 设计模式；
- 多层封装；
- 第三方框架；
- 复杂抽象。

如果简单方案已经能够解决问题，优先采用简单方案。

---

### 4. 新增依赖必须说明原因

添加新的 Python 库、工具或框架之前，需要说明：

- 它是什么；
- 为什么当前项目需要它；
- 它解决什么问题；
- 不使用它是否也能完成；
- 为什么最终选择它。

新技术应服务于当前任务和 ROADMAP 中的实际目标，不能只为“丰富技术栈”而引入。

---

### 5. 修改后必须实际验证

任何修改都需要执行与改动类型相匹配的验证：

- 修改代码或测试时，执行相关测试；
- 修改配置时，验证配置能够被实际加载或执行；
- 只修改文档时，检查内容、链接、格式和 `git diff`，不虚构不适用的测试结果。

最终需要说明执行了什么命令、验证了哪些内容、结果是否通过；如失败，需要说明已确认的原因。没有实际验证时，不得直接声称修改正确。

---

## 四、学习说明与代码注释规则

### 1. 新概念说明

解释新的技术概念时，尽量结合当前项目回答：

1. 它是什么；
2. 为什么这个项目需要它；
3. 它解决了什么问题；
4. 如果不用它，有什么替代方案和影响。

目标是形成能够复习、实践和面试表达的理解，而不是只记定义。

### 2. 学习代码注释

- 中文注释优先说明 **WHY**，解释设计原因、业务约束、风险或容易误解的行为；
- 测试函数尽量使用 `Arrange / Act / Assert / Cleanup` 表达职责边界；
- 新增或修改 fixture 时，需要解释其 `scope`、依赖关系以及使用 `yield` 管理 setup / teardown 的原因；
- 对初学者不直观的 Python 语法增加简短解释，例如 `**kwargs`、上下文管理或生成器行为；
- 不添加逐行翻译代码、重复函数名含义或描述显而易见操作的废话注释。

注释应帮助理解关键设计，但不能把源代码写成巨型教程；较完整的学习总结放入 `docs/LEARNING_LOG.md`。

---

## 五、阶段验收与文档同步规则

- Phase 状态只在 `docs/ROADMAP.md` 中维护；
- 必须实际执行该阶段对应的测试或验收命令，并确认结果后，才能把 Phase 标记为完成；
- Phase 收口时检查 `docs/ROADMAP.md`、`docs/LEARNING_LOG.md` 和 `README.md`，按真实结果同步阶段状态、实践成果和对外说明；
- Learning Log 只记录真实发生且已经确认的问题、新理解、认知纠正、值得避免的工程踩坑，以及有面试价值的实践；
- 普通文件创建、简单命令、无新知识的重复操作和尚未确认原因的问题，不写入 Learning Log；
- 更新文档或完成阶段后，不自动 commit 或 push，除非用户明确要求；
- 最终汇报必须包含测试或验收结果、实际修改的文件，以及推荐的 commit message。

Learning Log 中的记录应尽量包含：遇到的问题、原因、排查过程、解决方式和自己的理解；重要命令可以保留示例。

---

## 六、测试开发规则

### 测试设计

测试场景需要同时关注：

- 正常流程；
- 异常操作；
- 边界条件；
- 状态变化；
- 权限；
- 数据一致性；
- 历史数据；
- 重复操作。

---

### 自动化测试

自动化测试优先覆盖：

1. 稳定的核心业务；
2. 高频回归场景；
3. 接口层可以完成的验证；
4. 少量关键 UI 冒烟流程。

不要为了追求自动化用例数量，把所有功能都做成 UI 自动化。

---

### AI 辅助测试

AI 用于：

- 阅读和理解代码；
- 分析代码引用关系；
- 分析需求或代码变更影响范围；
- 辅助补充测试点；
- 辅助分析失败日志。

AI 输出只能作为：

**风险候选 / 分析建议**

不能直接作为最终 Bug 结论。

最终结果需要测试人员结合：

- 需求；
- 代码；
- 实际执行结果；

进行人工确认。

---

## 七、安全规则

禁止提交以下内容到 Git：

- 密码；
- Token；
- Cookie；
- 数据库密码；
- API Key；
- `.env`；
- 其他账号凭证或敏感信息。

不要修改 `.venv/`。虚拟环境只用于本地依赖隔离，不属于项目源码。

---

## 八、Git 使用规范

- Commit Message 保持简单、清晰，并与实际改动一致；
- 未经用户明确要求，不自动执行 `git commit` 或 `git push`；
- 提交前检查修改范围，避免混入凭证、运行产物或与当前任务无关的文件。

常用格式：

- `chore: ...` —— 项目配置、初始化、依赖调整；
- `docs: ...` —— 文档修改；
- `test: ...` —— 新增或修改测试；
- `feat: ...` —— 新增功能；
- `fix: ...` —— 修复问题；
- `refactor: ...` —— 代码重构。

示例：

```text
chore: initialize test project
docs: update collaboration rules
test: add login test cases
feat: add employee api client
refactor: extract login fixture
```
