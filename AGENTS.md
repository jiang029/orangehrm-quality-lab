# OrangeHRM Quality Lab - 项目协作说明

## 一、项目目标

本仓库是一个基于 OrangeHRM 的测试开发学习项目。

项目主要用于：

- 系统学习软件测试开发相关技术；
- 完成一个能够真实运行、能够在面试中完整讲解的测试项目；
- 掌握接口自动化、UI 自动化、数据库校验、持续集成及 AI 辅助测试等能力；
- 为测试开发岗位求职和面试做准备。

本项目的目标不仅是“把代码跑通”，还要求理解每一个重要设计：

- 为什么这样设计；
- 解决了什么问题；
- 是否存在其他方案；
- 面试时如何解释。

---

## 二、当前技术栈

当前已经使用：

- Python 3.12
- Pytest
- Git
- GitHub

后续会根据项目实际需求逐步引入：

- Requests
- Postman
- MySQL / MariaDB
- PyMySQL
- Docker / Docker Compose
- Playwright
- Allure
- GitHub Actions
- Codex
- AGENTS.md / Skill

注意：

**没有进入对应学习阶段之前，不要为了“技术栈更丰富”提前添加依赖或搭建复杂框架。**

---

## 三、项目开发原则

### 1. 小步迭代

每次只完成一个明确的小目标。

例如：

- 先跑通一个接口；
- 再封装公共请求；
- 再加入 Pytest；
- 再考虑数据驱动。

不要一次性生成完整自动化框架。

---

### 2. 修改代码前先说明

在修改文件之前，需要先说明：

1. 准备修改什么；
2. 为什么需要修改；
3. 会影响哪些文件；
4. 修改完成后如何验证。

---

### 3. 优先保证可理解性

本项目是学习和求职项目。

代码优先考虑：

- 清晰；
- 易懂；
- 容易解释；
- 容易维护。

不要为了体现“高级”而加入没有必要的：

- 设计模式；
- 多层封装；
- 第三方框架；
- 复杂抽象。

如果简单方案已经能够解决问题，优先采用简单方案。

---

### 4. 新增依赖必须说明原因

添加新的 Python 库、工具或框架之前，需要说明：

- 它是什么；
- 为什么当前项目需要它；
- 它解决什么问题；
- 不使用它是否也能完成；
- 为什么最终选择它。

---

### 5. 修改后必须验证

代码发生修改以后，应执行对应测试。

需要说明：

- 执行了什么命令；
- 哪些测试被执行；
- 是否通过；
- 如果失败，失败原因是什么。

不要在没有运行测试的情况下直接认为修改正确。

---

## 四、学习要求

解释新的技术概念时，尽量回答以下四个问题：

### 1. 它是什么？

用容易理解的方式解释概念。

### 2. 为什么这个项目需要它？

必须结合 OrangeHRM Quality Lab 当前实际场景。

### 3. 它解决什么问题？

说明引入之前存在什么痛点。

### 4. 如果不用它会怎么样？

说明替代方案以及可能产生的问题。

目标是最终能够在面试中用自己的语言解释，而不是只会背定义。

---

## 五、测试开发规则

### 测试设计

测试场景需要同时关注：

- 正常流程；
- 异常操作；
- 边界条件；
- 状态变化；
- 权限；
- 数据一致性；
- 历史数据；
- 重复操作。

---

### 自动化测试

自动化测试优先覆盖：

1. 稳定的核心业务；
2. 高频回归场景；
3. 接口层可以完成的验证；
4. 少量关键 UI 冒烟流程。

不要为了追求自动化用例数量，把所有功能都做成 UI 自动化。

---

### AI 辅助测试

AI 用于：

- 阅读和理解代码；
- 分析代码引用关系；
- 分析需求或代码变更影响范围；
- 辅助补充测试点；
- 辅助分析失败日志。

AI 输出只能作为：

**风险候选 / 分析建议**

不能直接作为最终 Bug 结论。

最终结果需要测试人员结合：

- 需求；
- 代码；
- 实际执行结果；

进行人工确认。

---

## 六、安全规则

禁止提交以下内容到 Git：

- 密码；
- Token；
- Cookie；
- 数据库密码；
- API Key；
- `.env`；
- 其他账号凭证。

不要修改：

- `.venv/`

虚拟环境只用于本地依赖隔离，不属于项目源码。

---

## 七、Git 使用规范

Commit Message 尽量保持简单清晰。

常用格式：

- `chore: ...` —— 项目配置、初始化、依赖调整
- `docs: ...` —— 文档修改
- `test: ...` —— 新增或修改测试
- `feat: ...` —— 新增功能
- `fix: ...` —— 修复问题
- `refactor: ...` —— 代码重构

示例：

```text
chore: initialize test project
docs: add business analysis
test: add login test cases
feat: add employee api client
refactor: extract login fixture